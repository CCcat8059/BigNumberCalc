#include <iostream>

#include "Variable.h"
#include "BigNumber.h"
#include "FrontEndAnalyze.h"

using namespace std;
int main(int argc, char* argv[])
{
	string input;
	while (getline(cin, input)) {
		init(input);
	return 0;
}